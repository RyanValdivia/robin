// Sin contenido a propósito — AppShell (montado en app/layout.tsx) es quien
// renderiza el panel real leyendo el pathname. Esta ruta solo existe para que
// /chat sea una URL válida (bookmark, back/forward, compartir el link).
export default function ChatRoute() {
  return null;
}
